// find the longest palindromic substring in s. You may assume that the maximum length of s is 1000.

//example: 
    // input: "babad"
    //output: "bab"
    //note: "aba" is also a valid answer

//example
    //input: "cbbd"
    //output: "bb"


    var longestPalindrome = function(s) {
        // need to update

        for(var i = 0; i < s.length; i ++) { // looping through each character

            //bool to check the palindrome is odd or even length
            //find out the length of palindrome

            // get the max length

            //if statement 

            // need to continue



        }

        
    }